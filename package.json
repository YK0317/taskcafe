{
  "name": "taskcafe",
  "version": "1.0.0",
  "description": "TaskCafe backend API server",
  "main": "src/app.js",
  "scripts": {
    "test": "jest --coverage --verbose --testTimeout=10000",
    "test:watch": "jest --watch",
    "test:ci": "jest --coverage --verbose --ci --reporters=default --reporters=jest-junit",
    "test:report": "jest --coverage --coverageReporters=text-lcov > coverage.lcov",
    "coverage": "nyc report --reporter=text-lcov | coveralls"
  },
  "keywords": [
    "taskcafe",
    "backend",
    "api",
    "nodejs",
    "express"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2",
    "mongoose": "^7.8.7"
  },
  "devDependencies": {
    "coveralls": "^3.1.1",
    "jest": "^29.0.0",
    "jest-junit": "^16.0.0",
    "nyc": "^15.1.0",
    "supertest": "^7.1.4"
  },
  "dependencies": {
    "joi": "^18.0.1",
    "jsonwebtoken": "^9.0.2"
  }
}
